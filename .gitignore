.DS_Store
node_modules
.turbo
*.log
.next
.vercel
.react-router
.trigger
.sst
dist
dist-ssr
*.local
.cache
server/dist
public/dist
vite.config.ts.timestamp*
.claude-flow
.claude/settings.local.json
.claude/scheduled_tasks.lock
.mcp.json
opencode.json
.windsurf

# Lingui compiled catalogs
packages/locale/locales/**/*.mjs

# Generated PDF font data (woff base64) — built on postinstall + turbo build
packages/documents/src/pdf/fonts.data.ts

scripts/migrations/data
scripts/paperless-test.ts
scripts/test-get-stripe-customer.ts

# local env files
.env
.envrc
.env.local
.env.dev
.env.development.local
.env.test.local
.env.prod
.env.production
.env.production.local
!packages/apps/**/.env
packages/database/.env
packages/database/supabase/seed.sql

.env*.local

# Rust
**/target/
**/*.rs.bk
**/pkg/

# C++ (cad-engine)
**/build/
!**/build/.gitkeep
.orcka
**/supabase/snippets

# Worktree-isolated dev environment
.carbon-worktree
.superpowers

# Agent scratch — local-only, never commit
.claude/scratch/
apps/*/portless.json
packages/database/supabase/snippets/**
.pr-review

# Loop run artifacts (bindings, ledgers, outcomes, screenshots) — runtime, not source.
# All per-run output lives under llm/loops/runs/; everything else in llm/ is docs.
# The ledger summary goes in the PR body; screenshots are hosted on the PR.
llm/loops/runs/

# Outer-loop scratch (design plans, the box's operating prompt) — kept out of the
# product tree; the design lives in PR history and the prompt lives on the OpenClaw box.
llm/outer-loop/*
!llm/outer-loop/README.md
