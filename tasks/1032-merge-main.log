Confirmed environmental, not a merge issue:

- `@carbon/logger` is a **real package** (`packages/logger/`) added by origin/main, but its `dist/` isn't built in this local checkout — hence `TS2307: Cannot find module`.
- Every failing file (e.g. `packages/react/src/utils/dom.ts`) comes **directly from origin/main** and imports `@carbon/logger` — proving these errors are pre-existing on main, unrelated to my conflict resolution.
- **None of the 5 files I resolved produced any type error.** CI builds packages in dependency order, so `@carbon/logger` resolves there (and PR CI was green pre-merge).

Task complete. Summary:

## ✅ Merge complete — PR #1096 unblocked

**Resolved 5 conflicts:**

| File | Resolution |
|------|-----------|
| `tool-metadata.json` | Union of both sides — newer timestamp, `totalTools: 1309` (verified actual unique tool count, no dupes) |
| `shared.service.ts` | Kept all imports: `trigger` + `NotificationEvent` (HEAD) and `getLogger` (main); all confirmed used |
| `purchasing.service.ts` | Main's canonical explicit-return-type pattern (`PostgrestResponse<…>` + `@ts-ignore`) — unrelated to the approvals feature |
| `sales.service.ts` | Same explicit-return-type pattern from main |
| `notify.ts` | Dropped HEAD's now-dead inline `getDescription` (650 lines); main refactored that logic into `content.ts`'s `getNotificationContent`, which the merged body already calls with `documentType` — all document-approval cases (JE, payment, purchase invoice, memo, PO) present in `content.ts` |

- `pnpm-lock.yaml` auto-merged cleanly (no conflict).
- Merge committed `bb58e1008`, **pushed** to `origin/loop/1032`.
- PR #1096: `mergeable: MERGEABLE` (was DIRTY/CONFLICTING). `BLOCKED` = awaiting review only.
- **CI kicked off** (Install + Vercel deploys running).
- Typecheck: merge introduced **zero** new errors; the only failures are pre-existing unbuilt-`@carbon/logger` module-resolution errors in untouched packages (build-order/environment, green in CI).
