# Task: Merge main into loop/1010-20260714010219 (PR #1137)

## Context
PR #1137 (Job Operation Batching, issue #1010) has merge conflicts with main.

Two commits landed on main after the PR's last push:
1. `27eef986f` — `fix(shared): repair PostgREST embeds broken by composite tenant FKs (#1144)` — touches `apps/erp/app/modules/shared/shared.service.ts`, `tool-metadata.json`, 4 supplier-part delete routes
2. `10c028759` / `3b156d12f` / `90b57798d` / `993a07573` — BOM effectivity removal — touches `packages/database/src/types.ts`, `packages/database/supabase/functions/lib/types.ts`, locale files, `get-method` function, items models

The PR branch also touches `tool-metadata.json` (batch planning metadata) and `packages/database/src/types.ts` / `packages/database/supabase/functions/lib/types.ts` (batch status + new columns).

## Objective
Merge `origin/main` into `loop/1010-20260714010219`, resolve any conflicts carefully preserving BOTH sides:
- Keep all batch-related additions from the PR (batch status enum with `Completing`, `jobOperationBatch*` columns/types, batch planning tool-metadata entries)
- Keep all changes from main (PostgREST embed fixes, BOM effectivity removal from types, new migration, locale changes)

## Steps

```bash
cd /home/openclaw/carbon
git fetch origin
git checkout loop/1010-20260714010219
git merge origin/main
```

If conflicts:
- `packages/database/src/types.ts` / `lib/types.ts`: merge both sides — keep BOM effectivity removal AND keep all batch-related additions
- `tool-metadata.json`: keep both the PostgREST fix and the batch planning entries
- Other files: accept the incoming main changes where the PR hasn't touched the same lines

After resolving:
```bash
# Typecheck
pnpm --filter @carbon/database typecheck 2>/dev/null || true
pnpm --filter erp typecheck
pnpm --filter mes typecheck

# Lint
pnpm --filter erp exec biome check . 2>/dev/null | tail -5 || true

git push origin loop/1010-20260714010219
```

## Outcome
- Push the merged branch so PR #1137 shows clean (no conflicts)
- Write outcome to `/home/openclaw/carbon/.ai/runs/1010-review-2/merge-main.txt` with result summary (conflicts resolved, typecheck status, push status)
- Do NOT open a new PR; the merge goes to the existing PR branch
