import { LoadDataView } from "@carbon/onboarding/ui";

// State, counts, and mutations come from <HubProvider> in the layout.
export default function GetStartedLoadDataRoute() {
  return <LoadDataView />;
}
