# Golden Record Validation QA

## 1) 采样与范围

- 仅读，不写。
- 目标：判断 `completedQuantity` 是否可由本地样本闭环定义，并验证 `1/1/0` 的语义一致性。
- 范围：`docs/ontology`、本地 Carbon Postgres、运行容器列表、映射测试套件。

## 2) 运行环境与版本

- 工作树：`E:\6.Factory OS\carbon-runtime\carbon\.worktrees\factory-os-golden-record-validation`
- 基线分支：`factory-os-golden-record-validation`
- Baseline commit：`42582bcd7d7b1699f250510314c886218bde8ef2`

## 3) 运行时核验（只读）

### 容器/服务

- `docker ps` 显示运行服务：`carbon-carbon-postgres-1`、`carbon-carbon-inbucket-1`、`carbon-carbon-kong-1`、`carbon-carbon-gotrue-1`、`storage`、`realtime`、`edge-runtime`、`inngest`、`redis`。
- 无 ERPNext/Frappe 运行容器。

### 数据面核验（PostgreSQL）

本地 Postgres 查询（只读）结果：

| 查询 | 结果 |
|---|---|
| `public.job` 行数 | 9 |
| `public."jobOperation"` 行数 | 109 |
| `public."productionQuantity"` 行数 | 3 |
| `public."productionEvent"` 行数 | 7 |
| `FOS-P11C0B-001` 样本 `job.quantity / productionQuantity / quantityComplete / scrapQuantity` | `1 / 1 / 0 / 0` |
| `FOS-P11C0B-001` 样本 operation 中 `Done` 且 complete 的行 | `operationQuantity=1`、`quantityComplete=1`、`quantityScrapped=0` |
| `productionQuantity` 备注 | `Generated by QR code` |
| `productionEvent` 时长 | `duration=19` |

### 代码/配置核验

- 容器与现有代码未发现可信的实时 ERPNext→Carbon 同步执行链条（只存在静态 schema/归档线索与映射语义层定义）。

## 4) 合约映射与回归验证

- 现有 P2 文档已确认：
  - 未发现可靠运行态 ERPNext Work Order → Carbon Job 关联；
  - `completedQuantity` 尚未定义为稳定规范；
  - `1:1:0` 回归已被明确列为需要领域确认。
- 现有 mapping MVP 测试通过：
  - `pnpm --filter @carbon/utils exec vitest run src/production-order-mapping.test.ts`：`1` file / `10` tests PASS
  - `pnpm --filter @carbon/utils typecheck`：PASS
  - `pnpm --filter @carbon/utils test`：`12` files / `155` tests PASS
  - `pnpm --filter @carbon/config build`：PASS（生成 `@carbon/config` vitest 配置产物）

## 5) EvidenceRecord 示例

```ts
// 示例：金属订单主事实（脱敏）
{
  id: "e-job-golden-001-main",
  source: {
    system: "carbon-mes",
    objectType: "Job",
    recordId: "JOB-GOLDEN-001",
    sourceRef: "public.job",
    retrievalMechanism: "read-only psql"
  },
  fact: {
    label: "jobQuantityComplete",
    value: 0,
    description: "source-level jobQuantityComplete (no ERPNext confirmed produced_qty)"
  },
  observedAt: "2026-08-15T01:10:56Z",
  retrievedAt: "2026-08-15T13:34:00+08:00",
  freshness: "unknown",
  confidence: "high",
  provenance: {
    model: "carbon-postgres",
    rule: "source-fact-no-cross-system-aggregation",
    sourceField: "public.job.quantityComplete"
  }
}
```

```ts
{
  id: "e-job-operation-golden-001-done",
  source: {
    system: "carbon-mes",
    objectType: "JobOperation",
    recordId: "OP-GOLDEN-001",
    sourceRef: "public.jobOperation",
    retrievalMechanism: "read-only psql"
  },
  fact: {
    label: "operationQuantityComplete",
    value: 1,
    description: "single completed operation in local job"
  },
  observedAt: "2026-08-15T01:13:13Z",
  retrievedAt: "2026-08-15T13:34:00+08:00",
  freshness: "unknown",
  confidence: "high",
  provenance: {
    model: "carbon-postgres",
    rule: "operation-is-source-only",
    sourceField: "public.jobOperation.quantityComplete"
  }
}
```

## 6) 结论与 P2 状态

- 该黄金记录的关键语义已得到确认：本地样本存在 `operation=1`、`job=0` 的真实形态，支持 `completedQuantity` 不能直接等于 `job.quantityComplete`。
- 但跨系统身份与权威链条未闭环，**整体仍为 P2 级别 BLOCKED（不启动 P2 实装）**。
