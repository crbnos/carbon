# Golden Record Data Sheet

## Scope

本轮仅做只读校验，不改数据库、不改服务、不改代码。

本地 Carbon 运行时当前唯一可观测生产订单样本来源于
`public.job` 表（本地 Postgres 内部样本），用于验证
Phase 11C.1A 是否已形成可执行的 `completedQuantity` 定义依据。

## Candidate Golden Record

| 维度 | ERPNext（预期字段） | Carbon MES（本地可观测） |
|---|---|---|
| 订单主键 | `WO-GOLDEN-001`（仅schema占位，未在本地运行时观测） | `JOB-GOLDEN-001`（来源于本地 `public.job.jobId = FOS-P11C0B-001`） |
| 行项目 ID | 未观测 | `PART-A`（来源于本地 `itemId`，为脱敏别名） |
| 计划数量 | 未观测（仅归档 schema） | `1` |
| 执行数量 | 未观测 | `1` |
| 作业完成量（job） | 未观测（本地无 ERPNext 侧） | `0`（`public.job.quantityComplete`） |
| 物料耗/废品 | 未观测 | `0`（`public.job.scrapQuantity`） |
| 状态 | 未观测（本地无 ERPNext 侧） | `In Progress`（`public.job.status`） |
| 关键时间 | 未观测 | `createdAt = 2026-08-15T01:10:56Z` |
| 证据信心 | 低（schema-only） | 中（单行可观测记录） |

## Carbon MES Source Evidence

来源路径与字段（仅本地只读）：

1. `public.job`
   - `jobId: FOS-P11C0B-001`
   - `quantity: 1`
   - `productionQuantity: 1`
   - `quantityComplete: 0`
   - `scrapQuantity: 0`
   - `status: In Progress`
   - `customFields: {}`、`notes: {}`（无 ERP 参考信息）
   - `createdAt: 2026-08-15T01:10:56Z`
2. `public.jobOperation`（关联上述 job）
   - 找到 1 条 `status = Done` 的操作：
   - `operationQuantity = 1`、`quantityComplete = 1`、`quantityScrapped = 0`
3. `public.productionQuantity`
   - 找到 1 条与上面操作关联的生产量：
   - `type = Production`、`quantity = 1`、`notes = "Generated by QR code"`
4. `public.productionEvent`
   - 找到 1 条与该操作关联的生产事件：
   - `type = Labor`、`duration = 19`

## ERPNext Source Evidence (本地运行态)

本地容器与服务清单未出现 ERPNext 容器；也未发现可直接读到的
`Work Order` 可用记录来源。`public.job` 未发现可承接 ERPNext
`Work Order` 的确定性外部键字段（不含 `external`, `erp`, `workOrder` 类字段）。

## Golden Record 适用性

本轮 Golden Record 为“本地可观测 Carbon 代理样本”，用于检验
`1:1:0` 的语义冲突与跨系统映射空缺；不是“真实 ERPNext ↔ Carbon 生产订单”完整闭环证据。
