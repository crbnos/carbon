{
  "name": "@carbon/workflows",
  "private": true,
  "version": "0.0.0",
  "sideEffects": false,
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "clean": "rimraf .turbo node_modules",
    "lint": "biome lint --write ",
    "test": "vitest run",
    "typecheck": "tsgo --noEmit"
  },
  "dependencies": {
    "@carbon/utils": "workspace:*",
    "@lingui/core": "catalog:",
    "zod": "catalog:"
  },
  "devDependencies": {
    "@carbon/config": "workspace:*",
    "@carbon/database": "workspace:*",
    "typescript": "catalog:",
    "vitest": "catalog:"
  }
}
