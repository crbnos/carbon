// Client-safe barrel: canonical types + pure inbound-resolution helpers.
// (No server-only imports — safe to pull into the sales module barrel.)
export * from "../types";
export * from "../validate";
