
## 15:42 UTC — Heartbeat

**Reconcile:** Issue #1005 had dead lockfile PID (1529990) with no open PR — stale lease recovered. Worktree removed, `agent:working` dropped, comment posted.

**PR feedback:** PR #986 (STEP import COOP/COEP headers for #450) — 1 review thread, fully resolved. No new feedback. Still in review.

**Dispatch:** Issue #1005 "Show remaining balance on order/invoice summary cards" — took lease, created worktree at `carbon-loop-1005`, wrote binding, dispatched `crbn up --minimal --run ...` (session dawn-zephyr, PID 1594841).

**Binding summary:** Pure UI feature. Add "Balance Remaining" row to 4 summary cards (SalesInvoiceSummary, PurchaseInvoiceSummary, SalesOrderSummary, PurchaseOrderSummary). DB views already have `balance` field. No migrations. Purchase order loader needs `invoiceSummary` added (mirroring sales order pattern).
