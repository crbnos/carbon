# Daily Notes — 2026-07-01

## 05:17 UTC — Heartbeat Pass

### Active Work
- **Issue #233 (Buy vs Pick Costing)** — `agent:working`
  - Previous stale build (05:16 run) failed: binding was written in wrong format (plain markdown headers instead of YAML frontmatter block — harness parser requires `---` frontmatter)
  - Fixed binding format with proper YAML frontmatter (`id`, `kind`, `risk`, `issue`, `title`, `acceptance[]`)
  - Relaunched: `crbn up --minimal --run '...'` → nova-rook session, /tmp/crbn-233.log
  - Env coming up: containers started, storage buckets initializing

### PR Status
- **PR #978 (ECOs / feat/carbon-plm)** — OPEN, CHANGES_REQUESTED (stale from 2026-06-29)
  - All CI checks ✅ (typecheck, lint, test, audit, Vercel previews)
  - All review threads resolved (0 unresolved)
  - Brad's CHANGES_REQUESTED review still open — all feedback addressed
  - Pinged Brad for re-review: https://github.com/crbnos/carbon/pull/978#issuecomment-4850495736

### Queue (blocked/groomed, not picked up this pass)
- #959 — Files missing on mobile screen sizes in MES (`agent:blocked`)
- #940 — RMAs (`agent:needs-decomposition`)
- #755 — NDA Badge (`agent:groomed`, `agent:blocked`)
- #450 — STEP file import freezes at 90% (no agent labels — unlabeled, unassigned to carbon-agent)
- #403 — Supabase legacy API key names (`agent:groomed`, `agent:blocked`)
- #391 — Browse Items by Shelf (`agent:groomed`, `agent:blocked`)

### Binding Fix Note
The harness `parseBinding()` requires YAML frontmatter block:
```
---
id: "233"
kind: feature
risk: med
issue: 233
title: "..."
acceptance:
  - "item 1"
  - "item 2"
---
```
Not plain markdown headers. Future bindings must use this format.

---

## 09:37 UTC — Heartbeat Pass

### Active Build
- **Issue #254** (Add non-conformances to search results) — loop started iteration 1 in `mild-pine` process (crbn up --minimal)
  - 2-file, ~10-line change: add `"issue"` and `"gauge"` to `EntityType` union + fill in `entityTypeConfig` styling

### Actions Taken
1. **PR #978**: Replied to unanswered barbinbrad review thread about docs build path fix (thread id 3492258763) — confirmed fix in commit 39afb3c
2. **Issue #450**: Added `agent:working` label — PR #986 (COOP/COEP headers for STEP import) is open, REVIEW_REQUIRED, CI ✅

### PR Status
- **PR #978 (ECOs)** — still CHANGES_REQUESTED (Brad hasn't re-reviewed); all threads now replied to
- **PR #986 (STEP import fix)** — OPEN, REVIEW_REQUIRED, CI ✅

### Queue
- #959 — `agent:blocked` (mobile MES)
- #940 — `agent:needs-decomposition` (RMAs)
- #755, #403, #391, #384, #310 — `agent:groomed`, `agent:blocked`

---

## 17:13 UTC — Heartbeat Pass

### Active Build
- **Issue #578** (Keypad Login in MES) — iteration 2 running (lock: /tmp/carbon-agent-578.lock, PID 1664664)
  - Iteration 1 complete: added PIN brute-force lockout primitives to `@carbon/auth pin.server.ts` (5 attempts → 15-min lock, covers AC[3])
  - Services up: erp :34349, mes :34519

### PR Status
- **PR #978 (ECOs)** — OPEN, CHANGES_REQUESTED (awaiting Brad re-review; all threads resolved, CI ✅)
- **PR #986 (STEP import COOP/COEP)** — OPEN, CI ✅, no unresolved threads, awaiting review/merge
- **PR #1006 (docs: fix stale conductor skill path)** — OPEN, CI ✅, no reviews yet

### No Action Needed This Pass
Build active; queue unchanged.
